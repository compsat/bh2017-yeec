<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.lirongsansy.projectdemeter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Initial">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".dietPlanner" />
        <activity android:name=".Information" />
        <activity
            android:name=".FoodTracker"
            android:label="@string/title_activity_food_tracker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Salmon" />
        <activity android:name=".Chicken" />
        <activity
            android:name=".FoodLister"
            android:label="@string/title_activity_food_lister"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".shetgago"
            android:label="@string/title_activity_shetgago"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>